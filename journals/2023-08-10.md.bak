- #cs-magic #dev
	- vscode
	- git <-- zsh <-- brew <-- raw.githubusercontent.com <--  config https_proxy: https://zhuanlan.zhihu.com/p/115450863 <-- clash
	- brew (path) <-- 
	  ```shell
	  (echo; echo 'eval "$(/opt/homebrew/bin/brew shellenv)"') >> /Users/yuanli/.zprofile
	   eval "$(/opt/homebrew/bin/brew shellenv)"
	  ```
	- oh-my-zsh (with prompt)
	- prisma <--postgresql (database + pgadmin) <-- yarn <-- npm (`brew install npm`)
	-