## note 1
- 【解决基本问题能力】
    
    - p1. nextjs 项目回滚后找不到 nextjs，在分析 package.json后它给出了`pnpm add next@15.3.1 --force` 的解决方案
        
        - p2. 第一个问题已经修复
            
- 【preview 功能】
    
    - p3. 独立的预览按钮，很棒，但 tab 有点 bug（背景色），分析过程非常可靠
        
    - p5. 但这个功能在改了一些后，再次点击，不是很稳定（我需要 reload window 才可以让它重新触发一次 preview 的动作）
        
    - 另外，在 preview 页面里时，快捷键（cmd+shift+p）会被劫持，需要点击 tab 窗格或者其他区域才可以触发
        
    - p7. 在代码报错后，点击前端的复制按钮，结果被 block 了，值得注意的是，windsurf 也有这个问题（这个很烦，希望百度能高优先级解决一下，我找过 windsurf issue，他们还没有解决，也很慢）
        
    - 【强烈建议】preview 可以独立于 conversation，因为比较像是一个独立进程
        
- 【shell 自动补全能力】
    
    - p4. comate 的 shell 集成了相较于默认的 shell 更好的自动补全功能，不需要按 tab 键，而且建议看起来很不错。但有几个加强的点：
        
        - 1. 在按 cmd+K 清空 console 后，这个自动补全就没了，不知道为啥
            
        - 2. 在 suggestion 里，只有参数，没有解释，还不够友好（最好能自定义是否显示）
            
        - 3. UI 上也有一些问题（强调色部分看不清）
            
- 【tab 自动补全】
    
    - p8. 没有这个功能（是我没有开启这个功能吗？）
        
    - p9. 最后在 zulu 里完成了

## note 2

- 【agent回滚】
    
    - p1. 看起来是不支持回滚的，但在咨询了百度官方人员之后，才明白，在停止智能体生成回复（我都没意识到它其实没停）后，就可以回滚了，这个设计应该加强一下，应该任意时刻都可以回滚才对
        
- 【undo/redo】
    
    - p2.可以 undo
        
    - p3. undo 后还可以 redo（ windsurf undo 后就没了，需要手动 cmd+Z，感觉comate 的设计更好，比较一致）
        
- 【联网检索】
    
    - p4. chat panel 已支持
        
    - p6. 给它 web 地址后，生成建议，然后 apply
        
    - p5. agent panel 目前不支持，百度官方回复未来会支持融合，现在团队是独立的（agent 是今年刚出的，还在不断融合一些最新的功能）